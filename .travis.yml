language: node_js
node_js:
  - "0.12"
before_script:
  - npm install -g grunt-cli
  - npm install -g web-component-tester
  - grunt build
script:
  - wct --plugins sauce
after_success:
  - ./configs/deploy.sh